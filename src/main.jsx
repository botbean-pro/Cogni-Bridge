import React, { useEffect, useMemo, useState } from "react";
import { createRoot } from "react-dom/client";
import { Accessibility, ArrowLeft, BookOpen, CalendarDays, CheckCircle2, ChevronRight, Clock3, Eye, FileText, Home, Languages, LogIn, LogOut, MessageCircle, Plus, ShieldCheck, Sparkles, Upload, UserRound, Users, X } from "lucide-react";
import "./styles.css";
import logoWithoutText from "./assets/logo without text.svg";

const MENTOR_EMAIL = "priya@cognibridge.com";
const MENTOR_PASSWORD = "password123";
const DEMO_STUDENT_EMAIL = "student@cognibridge.com";
const DEMO_STUDENT_PASSWORD = "student123";
const LOGO_PATH = logoWithoutText;

const initialSessions = [
  { id: "algebra-basics", subject: "Maths", title: "Algebra Basics", date: "2026-09-22", time: "16:00", endTime: "17:00", meetLink: "https://meet.google.com/algebra-basics", attendees: 12, description: "Build confidence with variables, expressions, and simple equations.", learn: ["Identify variables and constants", "Simplify basic algebraic expressions", "Solve one-step equations together"] },
  { id: "fractions-workshop", subject: "Maths", title: "Fractions Workshop", date: "2026-09-26", time: "10:00", endTime: "11:00", meetLink: "https://meet.google.com/fractions-workshop", attendees: 8, description: "Use visual models and practical examples to compare and work with fractions.", learn: ["Compare fractions using visual models", "Add and subtract like fractions", "Apply fractions to everyday problems"] }
];
const navigation = [[Home, "Home", "home"], [CalendarDays, "Sessions", "sessions"], [Sparkles, "Cogni-Flow AI", "flow"], [MessageCircle, "Messages", "messages"]];
const subjects = ["Maths", "Science", "English", "SST", "Languages"];
const indianLanguages = [["English", "English"], ["हिन्दी", "Hindi"], ["বাংলা", "Bengali"], ["తెలుగు", "Telugu"], ["मराठी", "Marathi"], ["தமிழ்", "Tamil"], ["ગુજરાતી", "Gujarati"], ["ಕನ್ನಡ", "Kannada"], ["മലയാളം", "Malayalam"], ["ਪੰਜਾਬੀ", "Punjabi"]];
const classNames = (...names) => names.filter(Boolean).join(" ");
const formatSession = (s) => `${new Date(`${s.date}T${s.time}`).toLocaleDateString("en-IN", { weekday: "short", month: "short", day: "numeric" })}, ${s.time} – ${s.endTime}`;
const readStudents = () => { try { const value = JSON.parse(localStorage.getItem("cognibridge_students") || "[]"); return Array.isArray(value) ? value : []; } catch { return []; } };

function LogoImage({ size = 28 }) {
  const [hasLogo, setHasLogo] = useState(true);
  return hasLogo ? <img src={LOGO_PATH} alt="" width={size} height={size} onError={() => setHasLogo(false)} /> : <Sparkles size={size} aria-hidden="true" />;
}
function AnimatedLogo() {
  return (
    <div className="intro-mark" aria-hidden="true">
      <span className="intro-half intro-left">
        <img src={logoWithoutText} alt="" width={140} height={140} />
      </span>
      <span className="intro-half intro-right">
        <img src={logoWithoutText} alt="" width={140} height={140} />
      </span>
    </div>
  );
}

function MouseTrail() {
  useEffect(() => {
    const circle = document.createElement('div');
    circle.className = 'mouse-trail-circle';
    document.body.appendChild(circle);

    const handleMouseMove = (e) => {
      circle.style.left = `${e.clientX}px`;
      circle.style.top = `${e.clientY}px`;
    };

    document.addEventListener('mousemove', handleMouseMove);

    return () => {
      document.removeEventListener('mousemove', handleMouseMove);
      circle.remove();
    };
  }, []);

  return null;
}

function App() {
  const [sessions, setSessions] = useState(initialSessions), [signedIn, setSignedIn] = useState(false), [registeredSessionIds, setRegisteredSessionIds] = useState([]), [subjectFilter, setSubjectFilter] = useState("All subjects"), [activeTab, setActiveTab] = useState("home"), [selectedSession, setSelectedSession] = useState(null), [loginOpen, setLoginOpen] = useState(false), [mentorOpen, setMentorOpen] = useState(false), [accessibilityOpen, setAccessibilityOpen] = useState(false), [appearance, setAppearance] = useState("light"), [textScale, setTextScale] = useState(1), [studentLanguage, setStudentLanguage] = useState("English"), [introExiting, setIntroExiting] = useState(false), [showIntro, setShowIntro] = useState(true);
  const [signupEmail, setSignupEmail] = useState(""), [toast, setToast] = useState("");
  useEffect(() => { const exitTimer = setTimeout(() => setIntroExiting(true), 1250); const removeTimer = setTimeout(() => setShowIntro(false), 1800); return () => { clearTimeout(exitTimer); clearTimeout(removeTimer); }; }, []);
  const filteredSessions = useMemo(() => subjectFilter === "All subjects" ? sessions : sessions.filter((s) => s.subject === subjectFilter), [sessions, subjectFilter]);
  const notify = (message) => { setToast(message); window.setTimeout(() => setToast(""), 2800); };
  const registerForSession = (sessionId) => { if (registeredSessionIds.includes(sessionId)) return; const session = sessions.find((item) => item.id === sessionId); setRegisteredSessionIds((ids) => ids.includes(sessionId) ? ids : [...ids, sessionId]); notify(`${session?.title || "Session"} registration confirmed`); };
  const logOut = () => { setSignedIn(false); notify("You have been logged out"); };
  const openSession = (session) => { setSelectedSession(session); setActiveTab("sessions"); };
  const goToTab = (tab) => { setActiveTab(tab); if (tab !== "sessions") setSelectedSession(null); };
  if (mentorOpen) return <MentorPortal sessions={sessions} initialLoggedIn onAddSession={(session) => setSessions((items) => [session, ...items])} onBack={() => setMentorOpen(false)} />;
  if (signupEmail) return <SignupPage email={signupEmail} onBack={() => setSignupEmail("")} onComplete={() => { setSignupEmail(""); setSignedIn(true); }} />;
  return <div className={classNames("app page-ready", `theme-${appearance}`)} style={{ "--text-scale": textScale }}>
    <MouseTrail />
    {showIntro && <div className={classNames("intro-screen", introExiting && "is-exiting")} aria-label="Loading CogniBridge"><AnimatedLogo /><p>CogniBridge</p></div>}
    <header className="sidebar"><div className="brand"><div className="brand-mark"><LogoImage size={30} /></div><div className="brand-copy"><strong>CogniBridge</strong><span>Learn together</span></div></div><nav className="nav" aria-label="Main navigation">{navigation.map(([Icon, label, tab]) => <button key={label} className={classNames("nav-item", activeTab === tab && "active")} onClick={() => goToTab(tab)}><Icon size={20} /><span>{label}</span></button>)}</nav><div className="top-actions">{signedIn ? <button className="profile-chip" onClick={logOut}><span className="avatar-small">A</span> Alex <LogOut size={16} /></button> : <button className="login-button" onClick={() => setLoginOpen(true)}><LogIn size={18} /> Sign in</button>}</div></header>
    <main className="main">{activeTab === "home" && <HomePage sessions={filteredSessions} subjectFilter={subjectFilter} setSubjectFilter={setSubjectFilter} signedIn={signedIn} registeredSessionIds={registeredSessionIds} onSignIn={() => setLoginOpen(true)} onRegister={registerForSession} onOpenSession={openSession} />}{activeTab === "sessions" && <SessionsPage sessions={sessions} selectedSession={selectedSession} signedIn={signedIn} registeredSessionIds={registeredSessionIds} onSelect={setSelectedSession} onBack={() => setSelectedSession(null)} onSignIn={() => setLoginOpen(true)} onRegister={registerForSession} />}{activeTab === "flow" && <FlowPage />}{activeTab === "messages" && <MessagesPage />}</main>
    <button className="accessibility-tab" aria-label="Open accessibility options" onClick={() => setAccessibilityOpen(!accessibilityOpen)}><Accessibility size={21} /><span>Accessibility</span></button>{accessibilityOpen && <AccessibilityPanel textScale={textScale} setTextScale={setTextScale} appearance={appearance} setAppearance={setAppearance} language={studentLanguage} setLanguage={setStudentLanguage} onClose={() => setAccessibilityOpen(false)} />}{loginOpen && <AuthModal onClose={() => setLoginOpen(false)} onStudentSuccess={() => { setLoginOpen(false); setSignedIn(true); }} onStudentEmail={(email) => { setLoginOpen(false); setSignupEmail(email); }} onMentor={() => { setLoginOpen(false); setMentorOpen(true); }} />}{toast && <div className="toast" role="status"><CheckCircle2 size={18} /> {toast}</div>}
  </div>;
}

function HomePage({ sessions, subjectFilter, setSubjectFilter, signedIn, registeredSessionIds, onSignIn, onRegister, onOpenSession }) { return <section className="content home-content"><section className="sessions-section"><div className="section-title"><div><p className="eyebrow">PLAN AHEAD</p><h2>Upcoming sessions</h2><p>Choose a session to see exactly what you will learn.</p></div><span className="session-count">{sessions.length} sessions</span></div><div className="filter-bar" aria-label="Filter sessions by subject"><span>Filter by subject</span><button className={subjectFilter === "All subjects" ? "selected" : ""} onClick={() => setSubjectFilter("All subjects")}>All subjects</button>{subjects.map((subject) => <button key={subject} className={subjectFilter === subject ? "selected" : ""} onClick={() => setSubjectFilter(subject)}>{subject}</button>)}</div>{sessions.length ? <div className="session-card-grid">{sessions.map((session) => <SessionCard key={session.id} session={session} signedIn={signedIn} registered={registeredSessionIds.includes(session.id)} onSignIn={onSignIn} onRegister={() => onRegister(session.id)} onOpen={() => onOpenSession(session)} />)}</div> : <div className="empty-state">No sessions found for this subject.</div>}</section>{signedIn && <StudentStats sessions={sessions} />}</section>; }
function SessionCard({ session, signedIn, registered, onSignIn, onRegister, onOpen }) { return <article className="session-card" onClick={onOpen} tabIndex="0" onKeyDown={(event) => event.key === "Enter" && onOpen()}><div className="session-card-top"><span className="subject-pill">{session.subject}</span><CalendarDays size={21} /></div><h3>{session.title}</h3><p className="session-description">{session.description}</p><p className="session-time"><Clock3 size={15} /> {formatSession(session)}</p><p className="session-learners"><Users size={15} /> {session.attendees + (registered ? 1 : 0)} learners signed up</p><div className="session-card-footer" onClick={(event) => event.stopPropagation()}>{registered ? <a className="meet-status meet-link" href={session.meetLink} target="_blank" rel="noopener noreferrer">Join Google Meet <ChevronRight size={15} /></a> : <span className="meet-status">{signedIn ? "Meet link unlocks after registration" : "Sign in to register"}</span>}<button className={classNames("secondary-btn", registered && "registered-button")} onClick={signedIn ? onRegister : onSignIn}>{registered ? "Registered" : signedIn ? "Register" : "Sign in"}</button></div><span className="card-detail-hint">View learning plan <ChevronRight size={14} /></span></article>; }

function SessionsPage({ sessions, selectedSession, signedIn, registeredSessionIds, onSelect, onBack, onSignIn, onRegister }) { return <section className="content sessions-page"><div className="section-title"><div><p className="eyebrow">LEARNING PLAN</p><h2>Sessions</h2><p>Open a session to see its learning plan and join when it is time.</p></div></div>{selectedSession ? <div className="session-detail"><button className="back-link" onClick={onBack}><ArrowLeft size={17} /> Back to Sessions</button><div className="detail-header"><div><span className="subject-pill">{selectedSession.subject}</span><h1>{selectedSession.title}</h1><p>{selectedSession.description}</p><p className="session-time"><Clock3 size={15} /> {formatSession(selectedSession)}</p></div><CalendarDays size={42} /></div><div className="detail-columns"><div><h3>What you’ll learn</h3><ul>{selectedSession.learn.map((item) => <li key={item}><CheckCircle2 size={18} /> {item}</li>)}</ul></div><div className="detail-action"><strong>Ready to learn together?</strong><span>{selectedSession.attendees} learners are signed up.</span><button className="primary-btn" onClick={signedIn ? () => onRegister(selectedSession.id) : onSignIn}>{registeredSessionIds.includes(selectedSession.id) ? "Registered" : signedIn ? "Register for session" : "Sign in to register"}</button>{registeredSessionIds.includes(selectedSession.id) && <a href={selectedSession.meetLink} target="_blank" rel="noopener noreferrer">Open Google Meet <ChevronRight size={15} /></a>}</div></div></div> : <div className="my-session-list">{sessions.map((session) => <button key={session.id} className="my-session-row" onClick={() => onSelect(session)}><span className="subject-pill">{session.subject}</span><span><strong>{session.title}</strong><small>{formatSession(session)}</small></span><ChevronRight size={18} /></button>)}</div>}</section>; }
function FlowPage() { return <section className="content feature-page"><div className="feature-hero"><span className="feature-icon"><Sparkles size={28} /></span><p className="eyebrow">COGNI-FLOW AI</p><h2>Your AI learning buddy</h2><p>Get help breaking down a tricky idea, practise with guided questions, and turn your notes into a simple study plan.</p><span className="coming-soon">Coming soon</span></div><div className="feature-grid"><article><Sparkles size={22} /><h3>Explain it simply</h3><p>Ask for a friendly explanation at the right level for you.</p></article><article><BookOpen size={22} /><h3>Practise together</h3><p>Work through examples with hints instead of instant answers.</p></article><article><CheckCircle2 size={22} /><h3>Track your progress</h3><p>Build small, consistent learning habits across your sessions.</p></article></div></section>; }
function MessagesPage() { return <section className="content feature-page"><div className="empty-feature"><MessageCircle size={32} /><p className="eyebrow">MESSAGES</p><h2>Stay connected with your mentor</h2><p>Mentor messages and session reminders will appear here.</p><button className="primary-btn">Messages coming soon</button></div></section>; }
function StudentStats({ sessions }) { return <section className="stats-section"><div className="section-title"><div><p className="eyebrow">YOUR PROGRESS</p><h2>Student stats</h2><p>Small steps add up. Keep going, Alex.</p></div></div><div className="stats-grid"><article><span className="stat-icon"><CheckCircle2 size={21} /></span><strong>8</strong><span>Sessions attended</span></article><article><span className="stat-icon"><Clock3 size={21} /></span><strong>6.5h</strong><span>Learning time</span></article><article><span className="stat-icon"><BookOpen size={21} /></span><strong>{new Set(sessions.map((s) => s.subject)).size}</strong><span>Subjects explored</span></article><article><span className="stat-icon"><Sparkles size={21} /></span><strong>4</strong><span>Day learning streak</span></article></div></section>; }
function LanguagePicker({ value, onChange }) { return <label className="language-picker"><span><Languages size={18} /><strong>Language</strong></span><select value={value} onChange={(event) => onChange(event.target.value)} aria-label="Choose learning language">{indianLanguages.map(([native, english]) => <option key={english} value={english}>{native}</option>)}</select><small>Choose the language used for learning support.</small></label>; }

function AuthModal({ onClose, onStudentSuccess, onStudentEmail, onMentor }) {
  const [tab, setTab] = useState("student"), [email, setEmail] = useState(""), [password, setPassword] = useState(""), [error, setError] = useState("");
  const googleClientId = import.meta.env.VITE_GOOGLE_CLIENT_ID;
  useEffect(() => { if (tab !== "student" || !googleClientId || document.querySelector("script[data-google-identity]")) return; const script = document.createElement("script"); script.src = "https://accounts.google.com/gsi/client"; script.async = true; script.defer = true; script.dataset.googleIdentity = "true"; document.head.appendChild(script); }, [tab, googleClientId]);
  const changeTab = (nextTab) => { setTab(nextTab); setError(""); if (nextTab === "mentor") { setEmail(MENTOR_EMAIL); setPassword(MENTOR_PASSWORD); } else { setEmail(""); setPassword(""); } };
  const submit = (event) => { event.preventDefault(); if (tab === "mentor") return email.trim().toLowerCase() === MENTOR_EMAIL && password === MENTOR_PASSWORD ? onMentor() : setError("Incorrect mentor email or password."); if (tab === "admin") return setError("Admin access is not enabled in this prototype yet."); if (!email.trim() || !password.trim()) return setError("Please enter your email and password to continue."); if (email.trim().toLowerCase() === DEMO_STUDENT_EMAIL && password === DEMO_STUDENT_PASSWORD) return onStudentSuccess(); const students = readStudents(); const student = students.find((item) => item.email.toLowerCase() === email.trim().toLowerCase()); if (student) return onStudentSuccess(); onStudentEmail(email.trim().toLowerCase()); };
  const googleSignIn = () => { if (!googleClientId) return setError("Add VITE_GOOGLE_CLIENT_ID to .env.local, then restart the dev server."); if (!window.google?.accounts?.id) return setError("Google sign-in is still loading. Please try again."); window.google.accounts.id.initialize({ client_id: googleClientId, callback: () => onStudentSuccess() }); window.google.accounts.id.prompt(); };
  return <div className="modal-backdrop" onMouseDown={onClose}><section className="auth-modal" role="dialog" aria-modal="true" aria-labelledby="login-title" onMouseDown={(event) => event.stopPropagation()}><button className="icon-btn modal-close" onClick={onClose} aria-label="Close sign in"><X size={20} /></button><div className="auth-icon"><UserRound size={24} /></div><p className="eyebrow">COGNIBRIDGE ACCESS</p><h2 id="login-title">Sign in to continue</h2><div className="auth-tabs" role="tablist">{[["student", "Student login"], ["mentor", "Mentor login"], ["admin", "Admin login"]].map(([key, label]) => <button key={key} className={tab === key ? "active" : ""} onClick={() => changeTab(key)} role="tab" aria-selected={tab === key}>{label}</button>)}</div><p className="modal-copy">{tab === "student" ? "Create an account or sign in with your email, or continue with Google." : tab === "mentor" ? "Priya Sharma’s Maths sessions are managed from the mentor space." : "Admin tools will be connected after the admin API is provided."}</p><form className="auth-form" onSubmit={submit}><label>Email<input type="email" value={email} onChange={(event) => setEmail(event.target.value)} placeholder={tab === "mentor" ? MENTOR_EMAIL : "you@example.com"} required /></label><label>Password<input type="password" value={password} onChange={(event) => setPassword(event.target.value)} placeholder="Enter your password" required /></label>{error && <div className="error-popup" role="alert"><ShieldCheck size={18} />{error}</div>}<button className="primary-btn" type="submit">{tab === "student" ? "Create account / sign in" : `Continue as ${tab}`} <ChevronRight size={18} /></button>{tab === "student" && <><small className="demo-login">Demo login: {DEMO_STUDENT_EMAIL} / {DEMO_STUDENT_PASSWORD}</small><div className="auth-divider"><span>or</span></div><button type="button" className="google-button" onClick={googleSignIn}><span>G</span> Continue with Google</button></>}{tab === "mentor" && <small className="demo-login">Demo login: {MENTOR_EMAIL} / {MENTOR_PASSWORD}</small>}</form></section></div>;
}
function AccessibilityPanel({ textScale, setTextScale, appearance, setAppearance, language, setLanguage, onClose }) { return <aside className="accessibility-panel" aria-label="Accessibility options"><div className="panel-heading"><strong>Accessibility</strong><button className="icon-btn" onClick={onClose} aria-label="Close accessibility options"><X size={18} /></button></div><label className="range-row"><span><Eye size={17} /> Text size</span><input type="range" min=".85" max="1.35" step=".05" value={textScale} onChange={(event) => setTextScale(Number(event.target.value))} /></label><LanguagePicker value={language} onChange={setLanguage} /><label className="appearance-row"><strong>Appearance</strong><select value={appearance} onChange={(event) => setAppearance(event.target.value)}><option value="light">Light mode</option><option value="dark">Dark mode</option></select></label><button className="reset-button" onClick={() => setTextScale(1)}>Reset text size</button></aside>; }

function SignupPage({ email, onBack, onComplete }) { const [form, setForm] = useState({ name: "", studentClass: "", age: "" }); const submit = (event) => { event.preventDefault(); try { const students = readStudents(); localStorage.setItem("cognibridge_students", JSON.stringify([...students, { ...form, email, createdAt: new Date().toISOString() }])); onComplete(); } catch { /* Demo storage may be unavailable in private browsing. */ } }; return <main className="signup-page"><button className="mentor-back" onClick={onBack}><ArrowLeft size={18} /> Back to sign in</button><section className="signup-card"><div className="auth-icon"><UserRound size={24} /></div><p className="eyebrow">NEW STUDENT PROFILE</p><h1>Let’s set up your learning space</h1><p className="modal-copy">We couldn’t find <strong>{email}</strong> yet. Add a few details so CogniBridge can personalise your experience.</p><form className="auth-form" onSubmit={submit}><label>Full name<input value={form.name} onChange={(event) => setForm({ ...form, name: event.target.value })} placeholder="Your name" required /></label><div className="signup-columns"><label>Class<input value={form.studentClass} onChange={(event) => setForm({ ...form, studentClass: event.target.value })} placeholder="e.g. 8" required /></label><label>Age<input type="number" min="5" max="25" value={form.age} onChange={(event) => setForm({ ...form, age: event.target.value })} placeholder="e.g. 13" required /></label></div><div className="quiz-placeholder"><Sparkles size={19} /><div><strong>Learning-style quiz — coming next</strong><p>We’ll use a short quiz later to suggest the best notes and study formats for you.</p></div></div><button className="primary-btn" type="submit">Create student profile <ChevronRight size={18} /></button></form></section></main>; }

function MentorPortal({ sessions, initialLoggedIn = false, onAddSession, onBack }) {
  const [loggedIn, setLoggedIn] = useState(initialLoggedIn), [tab, setTab] = useState("overview"), [email, setEmail] = useState(""), [password, setPassword] = useState(""), [error, setError] = useState(""), [mentorNotice, setMentorNotice] = useState(""), [notes, setNotes] = useState([]), [form, setForm] = useState({ title: "", subject: "Maths", date: "", time: "", endTime: "", meetLink: "" }), [noteForm, setNoteForm] = useState({ title: "", subject: "Maths", file: null });
  const upcomingSessions = sessions.filter((session) => session.date >= "2026-09-18");
  const submitLogin = (event) => { event.preventDefault(); if (email.trim().toLowerCase() === MENTOR_EMAIL && password === MENTOR_PASSWORD) { setLoggedIn(true); setError(""); } else setError("Incorrect mentor email or password."); };
  const publish = (event) => { event.preventDefault(); onAddSession({ ...form, id: String(Date.now()), attendees: 0, description: "A focused Maths session led by Priya Sharma.", learn: ["Understand the core idea", "Work through guided examples", "Practise independently"] }); setForm({ title: "", subject: "Maths", date: "", time: "", endTime: "", meetLink: "" }); setTab("overview"); };
  const uploadNote = (event) => { event.preventDefault(); if (!noteForm.file) return; const allowed = /\.(pdf|doc|docx|png|jpe?g)$/i.test(noteForm.file.name); if (!allowed || noteForm.file.size > 10 * 1024 * 1024) return; setNotes((items) => [{ ...noteForm, id: String(Date.now()), fileName: noteForm.file.name }, ...items]); setNoteForm({ title: "", subject: "Maths", file: null }); event.target.reset(); };
  const logoutMentor = () => { setLoggedIn(false); setMentorNotice("You have been logged out"); };
  if (!loggedIn) return <main className="mentor-login-page"><header className="sidebar mentor-login-topbar"><button className="mentor-wordmark" onClick={onBack}><span><LogoImage size={23} /></span><strong>CogniBridge <small>Mentor space</small></strong></button><button className="mentor-login-back" onClick={onBack}><ArrowLeft size={18} /> Back to learner view</button></header><section className="mentor-login-card"><p className="mentor-kicker">COGNIBRIDGE</p><h1>Mentor login</h1><p>Only mentors can access the protected mentor space.</p><form className="mentor-login-form" onSubmit={submitLogin}><label>Email<input type="email" value={email} onChange={(event) => setEmail(event.target.value)} placeholder={MENTOR_EMAIL} required /></label><label>Password<input type="password" value={password} onChange={(event) => setPassword(event.target.value)} placeholder={MENTOR_PASSWORD} required /></label>{error && <div className="error-popup" role="alert">{error}</div>}<button className="primary-btn" type="submit">Enter mentor space <ChevronRight size={18} /></button></form><small>Demo login: {MENTOR_EMAIL} / {MENTOR_PASSWORD}</small></section>{mentorNotice && <div className="toast" role="status"><CheckCircle2 size={18} /> {mentorNotice}</div>}</main>;
  return <main className="mentor-page"><header className="mentor-header"><button className="mentor-wordmark" onClick={onBack}><span><LogoImage size={23} /></span><strong>CogniBridge <small>Mentor space</small></strong></button><div><span className="mentor-user"><UserRound size={17} /> Priya Sharma · Maths</span><button className="mentor-logout" onClick={logoutMentor}><LogOut size={17} /> Log out</button></div></header><div className="mentor-layout"><aside className="mentor-nav"><p>MENTOR SPACE</p><button className={tab === "overview" ? "active" : ""} onClick={() => setTab("overview")}><Home size={18} /> Upcoming sessions</button><button className={tab === "schedule" ? "active" : ""} onClick={() => setTab("schedule")}><CalendarDays size={18} /> Schedule a session</button><button className={tab === "notes" ? "active" : ""} onClick={() => setTab("notes")}><FileText size={18} /> Upload notes</button></aside><section className="mentor-content">{tab === "overview" && <><div className="mentor-hero"><div><p className="mentor-kicker">MENTOR DASHBOARD</p><h1>Good morning, Priya.</h1><span>Only your upcoming Maths sessions are shown here.</span></div><button className="mentor-primary" onClick={() => setTab("schedule")}><Plus size={18} /> Schedule session</button></div><div className="mentor-stats"><article><CalendarDays size={21} /><strong>{upcomingSessions.length}</strong><span>Upcoming sessions</span></article><article><Users size={21} /><strong>{upcomingSessions.reduce((sum, session) => sum + session.attendees, 0)}</strong><span>Total sign-ups</span></article><article><FileText size={21} /><strong>{notes.length}</strong><span>Notes uploaded</span></article></div><h2 className="mentor-title">Upcoming sessions</h2><div className="mentor-session-grid">{upcomingSessions.map((session) => <article className="mentor-session-card" key={session.id}><span className="mentor-subject">{session.subject}</span><p className="mentor-time"><Clock3 size={14} /> {formatSession(session)}</p><h3>{session.title}</h3><p className="mentor-attendee-count"><Users size={16} /> {session.attendees} learners signed up</p><a href={session.meetLink} target="_blank" rel="noopener noreferrer">Open Google Meet <ChevronRight size={15} /></a></article>)}</div></>}{tab === "schedule" && <><p className="mentor-kicker">NEW SESSION</p><h1 className="mentor-title">Schedule a session</h1><p className="mentor-subtitle">Published sessions appear on the learner home page right away.</p><form className="schedule-form" onSubmit={publish}><label>Session title<input value={form.title} onChange={(event) => setForm({ ...form, title: event.target.value })} placeholder="e.g. Geometry foundations" required /></label><label>Subject<select value={form.subject} onChange={(event) => setForm({ ...form, subject: event.target.value })}><option>Maths</option></select></label><div className="form-columns"><label>Date<input type="date" value={form.date} onChange={(event) => setForm({ ...form, date: event.target.value })} required /></label><label>Start time<input type="time" value={form.time} onChange={(event) => setForm({ ...form, time: event.target.value })} required /></label><label>End time<input type="time" value={form.endTime} onChange={(event) => setForm({ ...form, endTime: event.target.value })} required /></label></div><label>Google Meet URL<input type="url" value={form.meetLink} onChange={(event) => setForm({ ...form, meetLink: event.target.value })} placeholder="https://meet.google.com/..." required /></label><button className="mentor-primary" type="submit"><CalendarDays size={18} /> Publish session</button></form></>}{tab === "notes" && <><p className="mentor-kicker">LEARNING MATERIALS</p><h1 className="mentor-title">Upload notes</h1><p className="mentor-subtitle">Add a subject so learners can find the right notes after a session.</p><form className="schedule-form notes-form" onSubmit={uploadNote}><label>Notes title<input value={noteForm.title} onChange={(event) => setNoteForm({ ...noteForm, title: event.target.value })} placeholder="e.g. Algebra practice sheet" required /></label><label>Subject<select value={noteForm.subject} onChange={(event) => setNoteForm({ ...noteForm, subject: event.target.value })}>{subjects.map((subject) => <option key={subject}>{subject}</option>)}</select></label><label className="file-upload"><span><Upload size={18} /> Choose file</span><input type="file" accept=".pdf,.doc,.docx,.png,.jpg,.jpeg" onChange={(event) => setNoteForm({ ...noteForm, file: event.target.files?.[0] || null })} required /><small>{noteForm.file?.name || "PDF, DOC, or image (max 10 MB)"}</small></label><button className="mentor-primary" type="submit"><Upload size={18} /> Upload notes</button></form>{notes.length > 0 && <div className="uploaded-notes"><h2>Uploaded notes</h2>{notes.map((note) => <div key={note.id}><FileText size={18} /><span><strong>{note.title}</strong><small>{note.subject} · {note.fileName}</small></span></div>)}</div>}</>}</section></div></main>;
}

createRoot(document.getElementById("root")).render(<React.StrictMode><App /></React.StrictMode>);
